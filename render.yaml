services:
  - type: web
    name: instagram-marketing-planner
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: SESSION_SECRET
        sync: false
      - key: GROQ_API_KEY
        sync: false
      - key: IG_TOKENS
        sync: false
      - key: BASE_URL
        sync: false
      - key: NODE_ENV
        value: production
